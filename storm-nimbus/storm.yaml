# List of the hosts in the Zookeeper cluster.
# 'zookeeper' is the Docker Compose network reference.
storm.zookeeper.servers:
  - "zookeeper"

storm.zookeeper.port: 2181

nimbus.host: "nimbus"
nimbus.task.timeout.secs: 1800
